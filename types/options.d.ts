export interface AreaItem {
    id: number;
    name: string;
}

export interface BuildingItem {
    id: number;
    code: string;
    name: string;
}

export interface RoomsItem {
    id: number;
    code: string;
    name: string;
    user_code: string;
}